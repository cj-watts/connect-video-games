# connect-video-games
AWS connect integration with api.igdb.com to create a video game information service contact center

In order for the lambda function to run you need to have 2 parameters stored in ssm `twitch/client` and `twitch/secret` 
See https://api-docs.igdb.com/?javascript#account-creation for more details on what is needed
